import { HeroFullbleedImage } from "@/components/blocks/marketing/hero-fullbleed-image/hero-fullbleed-image"
import { heroFullbleedImageContent } from "@/components/blocks/marketing/hero-fullbleed-image/hero-fullbleed-image.content"

export default function HeroFullbleedImagePage() {
  return <HeroFullbleedImage content={heroFullbleedImageContent} />
}
