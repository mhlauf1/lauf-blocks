import { AuthSplit } from "@/components/blocks/app/auth-split/auth-split"
import { authSplitContent } from "@/components/blocks/app/auth-split/auth-split.content"

export default function AuthSplitPage() {
  return <AuthSplit content={authSplitContent} />
}
